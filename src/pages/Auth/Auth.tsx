import React from 'react'
import FirstTimeScreen from './FirstTimeScreen'
import Register from './Register/Register'

const Auth = () => {
    // TODO: define the logic for the first timers
    // eslint-disable-next-line no-constant-condition
    if (true) {
        return (
            <FirstTimeScreen />
        )
    }

    
    return (
        <Register />
    )
}

export default Auth