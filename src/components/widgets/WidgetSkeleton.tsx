import React from 'react';
import { Skeleton } from '../ui/skeleton';

const WidgetSkeleton = () => {
    return <Skeleton className='h-[200px] w-full animate-pulse' />;
};

export default WidgetSkeleton;
